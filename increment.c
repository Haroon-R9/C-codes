#include <stdio.h>
int main()
{
    int i=5;
    printf("%d\n", i++); // in i++ first the value of i will print then it adds 
    printf("%d\n", i); // if we write ++i then it will add first then it prints

    return 0;
}